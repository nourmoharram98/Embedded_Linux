# Embedded_Linux
#Task 3


the outcome of the file: 
1-When I run echo $HELLO it prints the value of the global variable host name
2-When I run echo $LOCAL it prints the output of command whoiam 
3- both the global and local variables in bashrc are global in the new created terminal
